# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets — only the .example template is committed
.env
.env.*
!.env.example

# Project-local Claude Code / MCP config (per-user, not source)
.mcp.json
.agents/

# smoke-test artifacts (screenshots, etc.) — scripts in _smoke/ are tracked
_smoke/out/
_smoke/.auth/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
